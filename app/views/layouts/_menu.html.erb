<ul>
<li><%= link_to 'Home', url_for(root_url) %></li>
<% if user_signed_in? %>
  <li><%= link_to 'Dashboard', url_for(dashboard_url(current_user.id)) %></li>
<% end %>
<li><%= link_to (div == 'lhs' ? 'Contact Us' : 'Contact'), url_for(contact_url) %></li>
<li><%= link_to 'Help', url_for(help_url) %></li>
</ul>
