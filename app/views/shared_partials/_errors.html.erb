<% if model.errors.any? %>
  <div class="row">
    <div class="span6 offset3">
      <div class="alert alert-error">
        <h4 class="alert-heading">
          <%= pluralize(model.errors.count, "error") %> prohibited this record from being saved:
        </h4>
        <ul>
          <% model.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
<% end %>
