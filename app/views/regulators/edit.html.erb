<h1>Editing regulator</h1>
<div class="row">
  <div class="span6">
    <%= render 'form' %>
  </div>
  <div class="span6">
    <table class="table">
    <% @types.each do |type| %>
      <tr>
        <th><%= type.description %></th>
        <td><%= type.type_count %></td>
      </tr>
    <% end %>
    </table>
  </div>
</div>
<%= link_to 'Show', @regulator %> |
<%= link_to 'Back', regulators_path %>
