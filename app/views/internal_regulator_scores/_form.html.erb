<%= form_for(@internal_regulator_score) do |f| %>
  <% if @internal_regulator_score.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@internal_regulator_score.errors.count, "error") %> prohibited this internal_regulator_score from being saved:</h2>

      <ul>
      <% @internal_regulator_score.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
