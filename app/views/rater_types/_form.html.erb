<%= form_for(@rater_type) do |f| %>
  <% if @rater_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rater_type.errors.count, "error") %> prohibited this rater_type from being saved:</h2>

      <ul>
      <% @rater_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :short_description %><br />
    <%= f.text_field :short_description %>
  </div>
  <div class="field">
    <%= f.label :long_description %><br />
    <%= f.text_field :long_description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
